% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{drp_estimate_}
\alias{drp_estimate_}
\title{Internal double regression and post-stratification (DRP) estimator}
\usage{
drp_estimate_(
  y_vec,
  w_vec,
  cells_resp,
  cells_target,
  target_counts,
  nfolds = 3,
  ...
)
}
\arguments{
\item{y_vec}{Numeric outcome vector (length = n_respondents)}

\item{w_vec}{Numeric weight vector (length = n_respondents)}

\item{cells_resp}{Data frame of covariate columns for respondents only}

\item{cells_target}{Data frame of covariate columns for target population only}

\item{target_counts}{Vector of target counts for each cell in \code{cells_target}}

\item{nfolds}{Integer. Number of cross-fitting folds (default: 3)}

\item{...}{Additional arguments to pass to xgboost}
}
\value{
A one-row data frame with columns estimate, se, method
}
\description{
Uses cross-fitted xgboost predictions as the outcome model. The
point estimate is the target-population mean of the cross-fitted predictions
plus the calibration-weighted mean of residuals (same GREG-style formula as
\code{\link{greg_estimate_}}). The SE uses a sandwich estimator based on
the cross-fitted residuals. Additional arguments in \code{...} are forwarded
to \code{\link{crossfit_xgboost}} and then to \code{xgboost::xgboost()}.
}
\keyword{internal}
