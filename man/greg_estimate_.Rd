% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{greg_estimate_}
\alias{greg_estimate_}
\title{Internal GREG (generalised regression) estimator}
\usage{
greg_estimate_(
  y_vec,
  w_vec,
  cells_resp,
  cells_target,
  target_counts,
  order,
  lambda_val,
  use_ridge = FALSE
)
}
\arguments{
\item{y_vec}{Numeric outcome vector (length = n_respondents)}

\item{w_vec}{Numeric weight vector (length = n_respondents)}

\item{cells_resp}{Data frame of covariate columns for respondents only}

\item{cells_target}{Data frame of covariate columns for target population only}

\item{target_counts}{Vector of target counts for each cell in \code{cells_target}}

\item{order}{Integer. Order of interactions for the regression model}

\item{lambda_val}{Scalar lambda value used (for labelling output)}

\item{use_ridge}{Logical. Use \code{cv.glmnet} ridge regression to fit
the outcome model. Default \code{FALSE}.}
}
\value{
A one-row data frame with columns estimate, se, lambda, method
}
\description{
The point estimate is the population-weighted mean of regression predictions
on the target cells plus the calibration-weighted mean of residuals:
\eqn{\hat{\mu} = \bar{y}_{\text{target}} + \sum_i w_i e_i / \sum_i w_i},
where \eqn{e_i = y_i - \hat{y}_i}. The SE uses a sandwich estimator based
on the same residuals. The outcome model is OLS (via \code{lm.fit}) by
default; linearly-dependent columns (NA coefficients) are dropped
symmetrically from both the respondent and target design matrices. When
\code{use_ridge = TRUE}, ridge regression is used instead via
\code{cv.glmnet} (\code{alpha = 0}, penalty at \code{lambda.min}).
}
\keyword{internal}
