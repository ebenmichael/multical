% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{crossfit_ridge}
\alias{crossfit_ridge}
\title{Estimate outcome model with cross-fitting using ridge regression}
\usage{
crossfit_ridge(X, y, target_X, nfolds = 3, ...)
}
\arguments{
\item{X}{Numeric matrix of respondent covariates (no intercept)}

\item{y}{Numeric vector of observed outcomes (length = nrow(X))}

\item{target_X}{Numeric matrix of target-population covariates (no intercept)}

\item{nfolds}{Integer. Number of cross-fitting folds (default: 3)}

\item{...}{Additional arguments forwarded to \code{glmnet::cv.glmnet()}}
}
\value{
A named list with:
\describe{
\item{pred}{Numeric vector of cross-fitted predictions for respondents}
\item{target_pred}{Numeric vector of averaged predictions for target cells}
}
}
\description{
Splits the respondent data into \code{nfolds} folds. For each fold, fits a
ridge regression model (via \code{cv.glmnet} with \code{alpha = 0}) on the
remaining folds and predicts on (a) the held-out fold and (b) all
target-population cells. The target-population predictions are averaged
across the \code{nfolds} models.
}
\keyword{internal}
